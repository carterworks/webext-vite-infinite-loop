import React from "react-dom";
import ReactDOM from "react-dom";

const mountNode = document.getElementById("app");
ReactDOM.render(
	<div>
		<h1>Hello world from React and the Index App.</h1>
	</div>,
	mountNode,
);
